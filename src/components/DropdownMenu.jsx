import React from "react";

const DropdownMenu = () => {
  const placeholderObj = {
    0: "until I log out",
    1: "half hour",
    2: "one hour",
    3: "one day",
    4: "one week",
  };

  return <div></div>;
};

export default DropdownMenu;
